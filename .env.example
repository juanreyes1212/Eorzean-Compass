# This file serves as an example for the required environment variables.
# Copy this file to .env.local for local development.
# The setup script (bun run setup) will do this for you if .env.local does not exist.

# The base URL of your application.
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Disable Next.js telemetry during development.
NEXT_TELEMETRY_DISABLED=1

# Optional Supabase keys for future database/auth features.
# SUPABASE_URL=
# SUPABASE_ANON_KEY=